%h1.fs-2 Owls

= render 'layouts/search_form', q: @q, path: owls_path

.card.shadow.my-3
  .card-header.d-flex.justify-content-between.align-items-center
    %div 一覧
    %div= link_to '新規作成', new_owl_path, class: 'btn btn-success'
  .card-body
    = render 'owls', q: @q, owls: @owls

= paginate @owls
