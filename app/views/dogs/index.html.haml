%h1.fs-2 Dogs

= render 'layouts/search_form', q: @q, path: dogs_path

.card.shadow.my-3
  .card-header.d-flex.justify-content-between.align-items-center
    %div 一覧
    %div= link_to '新規作成', new_dog_path, class: 'btn btn-success'
  .card-body
    = render 'dogs', q: @q, dogs: @dogs

= paginate @dogs
