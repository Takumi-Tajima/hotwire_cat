%h1.fs-2 Hedgehogs

= render 'layouts/search_form', q: @q, path: hedgehogs_path

.card.shadow.my-3
  .card-header.d-flex.justify-content-between.align-items-center
    %div 一覧
    %div= link_to '新規作成', new_hedgehog_path, class: 'btn btn-success'
  .card-body
    = render 'hedgehogs', q: @q, hedgehogs: @hedgehogs

= paginate @hedgehogs
